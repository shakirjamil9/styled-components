import styled from "styled-components";

interface Props {
    variant: string
}

const Typography = (props: Props) => {
        {props.variant === 'h1' ? styled.h1`
        
        ` : }
}

export default Typography